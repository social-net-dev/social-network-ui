meta {
  name: React to Comment
  type: http
  seq: 25
}

post {
  url: {{api_base}}/comments/{{comment_id}}/reactions/
  body: json
  auth: bearer
}

body:json {
  {
    "reaction": "LIKE"
  }
}

auth:bearer {
  token: {{auth_token}}
}
