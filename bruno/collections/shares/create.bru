meta {
  name: Share Post
  type: http
  seq: 27
}

post {
  url: {{api_base}}/posts/{{post_id}}/share/
  body: json
  auth: bearer
}

body:json {
  {
    "message": "Sharing this post"
  }
}

auth:bearer {
  token: {{auth_token}}
}
