meta {
  name: Mark Notification Read
  type: http
  seq: 39
}

post {
  url: {{api_base}}/notifications/{{notification_id}}/read/
  body: json
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}
