meta {
  name: Create Comment
  type: http
  seq: 18
}

post {
  url: {{api_base}}/comments/
  body: json
  auth: bearer
}

body:json {
  {
    "post_id": "{{post_id}}",
    "content_text": "Great post!"
  }
}

auth:bearer {
  token: {{auth_token}}
}
