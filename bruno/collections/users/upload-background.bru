meta {
  name: Upload Background
  type: http
  seq: 11
}

post {
  url: {{api_base}}/users/me/background/
  body: formdata
  auth: bearer
}

body:multipart-form {
  file: /path/to/background.jpg
}

auth:bearer {
  token: {{auth_token}}
}
