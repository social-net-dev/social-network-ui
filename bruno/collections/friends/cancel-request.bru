meta {
  name: Cancel Friend Request
  type: http
  seq: 35
}

post {
  url: {{api_base}}/friends/requests/{{request_id}}/cancel/
  body: json
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}
